@page
@model SetPasswordModel
@{
    ViewData["Title"] = "Definir Senha";
    ViewData["ActivePage"] = ManageNavPages.ChangePassword;
}

<div class="container mt-4">
    <h3 class="text-primary fw-bold mb-3">@ViewData["Title"]</h3>

    <partial name="_StatusMessage" for="StatusMessage" />

    <div class="alert alert-info rounded-3 shadow-sm">
        <i class="bi bi-info-circle-fill me-2"></i>
        Você ainda não possui uma senha local neste site.
        Defina uma senha para poder acessar sua conta diretamente, sem precisar de login externo.
    </div>

    <div class="row mt-4">
        <div class="col-md-7">
            <div class="card shadow-sm border-0 rounded-4 p-4">
                <form id="set-password-form" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

                    <div class="form-floating mb-3">
                        <input asp-for="Input.NewPassword" class="form-control" autocomplete="new-password"
                               placeholder="Digite sua nova senha" />
                        <label asp-for="Input.NewPassword" class="form-label"></label>
                        <span asp-validation-for="Input.NewPassword" class="text-danger"></span>
                    </div>

                    <div class="form-floating mb-4">
                        <input asp-for="Input.ConfirmPassword" class="form-control" autocomplete="new-password"
                               placeholder="Confirme sua nova senha" />
                        <label asp-for="Input.ConfirmPassword" class="form-label"></label>
                        <span asp-validation-for="Input.ConfirmPassword" class="text-danger"></span>
                    </div>

                    <button type="submit" class="w-100 btn btn-lg btn-primary rounded-3 fw-semibold">
                        <i class="bi bi-lock-fill me-2"></i>Definir Senha
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}